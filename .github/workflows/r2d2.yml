name: Star Wars ASCII Warmup

on:
  push:
    branches:
      - main

jobs:
  print-r2:
    runs-on: ubuntu-latest   # FIX: required field
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run R2 script
        run: bash r2d2.sh
